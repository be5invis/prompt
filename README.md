# “That” fancy PowerShell prompt

To use:

- Install PSCore;
- Put the files in your profile directory;
- Install / update modules:
  - `posh-git`
  - `PSReadline`
  - `Get-ChildItemColor`

## LICENSE

MIT